import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
   inputWrapper: {
      width: '100%',
      flexDirection: 'row',
      alignItems: 'center',
      justifyContent: 'center',
   },

   checkBoxSquare: {
      marginRight: 15,
   },

   textInput: {
      fontSize: 15,
      color: '#FFFFFF',
      marginRight: 30,
   },
})
