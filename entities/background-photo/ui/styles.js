import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
   wrapper: {
      width: '100%',
      height: 100,
      marginBottom: 20,
   },

   image: {
      width: '100%',
      height: '100%',
      resizeMode: 'cover',
   },
})
